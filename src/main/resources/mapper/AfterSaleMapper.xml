<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xmu.wowoto.aftersale.mapper.AfterSaleMapper">

    <insert id="addAfterSale" parameterType="afterSalesService" useGeneratedKeys="true" keyProperty="id">
        insert into aftersales_service(gmt_create, gmt_modified, goods_type, apply_time, apply_reason, end_time, type, number, order_item_id)
        values (now(),now(),#{goodsType},now(),#{applyReason},#{endTime},#{type},#{number},#{orderItemId})  </insert>

</mapper>